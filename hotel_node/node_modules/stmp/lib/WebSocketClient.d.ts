/*!
 * Copyright 2019 acrazing <joking.young@gmail.com>. All rights reserved.
 * @since 2019-11-27 17:01:54
 */
import { STMPMessage } from './messages';
import { STMPClient, STMPClientOptions } from './STMPClient';
import { STMPInstance } from './types';
export declare class WebSocketClient extends STMPClient implements STMPInstance {
    static WebSocket: {
        new (url: string, protocols?: string | string[] | undefined): WebSocket;
        prototype: WebSocket;
        readonly CLOSED: number;
        readonly CLOSING: number;
        readonly CONNECTING: number;
        readonly OPEN: number;
    };
    readonly url: string;
    ws: WebSocket;
    constructor(url: string, headers?: Record<string, string>, options?: STMPClientOptions);
    protected terminate(): void;
    connect(remainCount?: number, previousStatus?: number | null, previousMessage?: string | null): Promise<[number, string]>;
    send(message: STMPMessage): void;
}
