/*!
 * Copyright 2019 acrazing <joking.young@gmail.com>. All rights reserved.
 * @since 2019-11-27 17:01:54
 */
/// <reference types="node" />
import net from 'net';
import { STMPMessage } from './messages';
import { STMPClient, STMPClientOptions } from './STMPClient';
import { StreamConsumer } from './StreamConsumer';
import { STMPInstance } from './types';
export declare class TCPClient extends STMPClient implements STMPInstance {
    readonly host: string;
    readonly port: number;
    readonly url: string;
    readonly consumer: StreamConsumer;
    socket: net.Socket;
    constructor(host: string, port: number, url?: string, headers?: Record<string, string>, options?: STMPClientOptions);
    private handleMessageData;
    connect(remainCount?: number, previousStatus?: number | null, previousMessage?: string | null): Promise<[number, string]>;
    send(message: STMPMessage): void;
    protected terminate(): void;
}
